/**
 * DEPRECATED — never mounted. Viewport-based mobile/desktop switching is
 * handled by components/layout/ViewportRouter.tsx (already in RootLayout);
 * during the June 2026 mobile-routing fix its mirror list was synced with the
 * real app/m directory instead of adding this second watcher.
 *
 * Safe to delete.
 */
export {};
